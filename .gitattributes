# Ensures all files use Unix-style line endings (LF) instead of Windows-style (CRLF)
# across all platforms, maintaining consistency in version control
* text eol=lf

# Treat image files as binary to prevent line ending conversions
*.png binary
*.jpg binary
*.jpeg binary